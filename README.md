# bagration-site

Repository root is a **container**. The Next.js application lives in **`/web`**.

## Running the app

```bash
cd web
npm install
npm run dev
```

Build and lint (e.g. in CI) run from the `web` directory (`working-directory: web`).
